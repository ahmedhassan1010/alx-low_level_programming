#include "main.h"

/**
 * _islower: checks for lowercase character
 *
 * Return: Always 0 (Success)
 */

int _islower(void)
{
	char r;

	if (islower(r))
	{
		_putchar('1');
	}
	else
		_putchar('0');
	_putchar('\n');

	return (0);
}
